import React from 'react';
import Club from '../Club/Club';
import Header from '../Header/Header';

const Home = () => {
    return (
        <div>
            <Header></Header>
            <Club></Club>
        </div>
    );
};

export default Home;